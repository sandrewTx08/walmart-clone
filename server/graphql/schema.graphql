### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Brands {
  id: ID!
  name: String!
}

type Carts {
  catalog_id: Int!
  image: String!
  name: String!
  price: Int!
  product_id: Int!
  quantity: Int!
  user_id: ID!
}

type Catalogs {
  Products: Products!
  id: ID!
  price: Int!
  product_id: ID!
}

type Mutation {
  cartAdd(catalog_id: Int!, quantity: Int!, user_id: ID!): Carts
  cartDelete(catalog_id: Int!, quantity: Int!, user_id: ID!): Carts
}

type ProductRates {
  Users: Users!
  _count: Int!
  description: String!
  id: ID!
  product_id: ID!
  rate: String!
  user_id: ID!
}

type ProductTypes {
  _count: Int!
  catalog(limit: Int!, store_id: Int): [Catalogs!]
  id: ID!
  name: String!
}

type Products {
  Brands: Brands!
  ProductRates(limit: Int): [ProductRates]!
  ProductTypes: ProductTypes!
  brand_id: ID!
  id: ID!
  name: String!
}

type Query {
  cart(user_id: ID!): [Carts]!
  catalog(catalog_id: Int!): Catalogs!
  department(department_id: Int!): ProductTypes!
  departments: [ProductTypes!]
}

type Users {
  first_name: String!
  id: ID!
}