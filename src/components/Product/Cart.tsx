import styled from "styled-components";

const C = styled.button`
  background-color: var(--WALMART-BLUE);
  color: white;
  padding: 1em;
  border: 0 solid;
  border-radius: 2em;
  position: absolute;
`;

export default C;
